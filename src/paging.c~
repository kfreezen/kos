#include <paging.h>
#include <common/bitset.h>

Bitset* pages;
PageDirectory pageDir;

void InitPaging(int mem_kb) {
	pages = kmalloc(sizeof(Bitset));
	int pagesAmount = mem_kb/4;
	pages->bitData = kmalloc(pagesAmount/32);
}
